# This file makes the 'models' directory a Python package.
# You could potentially import the db instance here if needed elsewhere,
# but models usually import it directly from the app initialization file.

# from app import db # Example if you wanted db accessible as models.db